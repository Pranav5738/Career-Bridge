# DevConnect Frontend

This folder contains the React + Vite frontend for Career-Bridge / DevConnect.

It provides the public site, authentication screens, dashboards, and feature pages that talk to the separate backend API in the repo root.
